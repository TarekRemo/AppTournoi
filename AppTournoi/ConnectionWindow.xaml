<Window x:Class="AppTournoi.ConnectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AppTournoi"
        mc:Ignorable="d"
        Title="Connexion en mode Gestionnaire" Height="200" Width="500" Background="#C2D9E2">
    <Grid Background="#C2D9E2" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <!-- Labels -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Login du gestionnaire" FontWeight="Bold" VerticalAlignment="Center" Margin="10"/>
        <TextBlock Grid.Row="3" Grid.Column="0" Text="Mot de passe" FontWeight="Bold" VerticalAlignment="Center" Margin="10"/>

        <!-- Champs de saisie -->
        <TextBox x:Name="TextBoxLogin" Grid.Row="0" Grid.Column="1" Margin="10" Height="20"/>
        <PasswordBox x:Name="PasswordBoxMDP" Grid.Row="3" Grid.Column="1" Margin="10" Height="20"/>

        <!-- Boutons -->
        <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="20">
            <Button x:Name="ButtonConnexion" Content="Connexion" Width="100" Height="30" Margin="10"/>
            <Button x:Name="ButtonAnnuler" Content="Annuler" Width="100" Height="30" Margin="10"/>
        </StackPanel>
    </Grid>
</Window>
